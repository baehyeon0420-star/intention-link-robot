# 2026-09-12 작업 정리 — Intention Link (EMG → 로봇손)

> AI·SW 인재페스티벌 출품 자료(11p PDF + 5분 영상) 준비를 위해 하루 동안 진행한 실험·수정·결론을 한 곳에 모은 기록.
> 오늘 만든 수집 도구·유효 데이터·세션 검증 스크립트는 이 저장소의 `emg_data_collection/`에 함께 커밋함.
> 기존 s1~s4(100Hz) 원본 데이터와 그걸 쓰는 실험 스크립트(`mvc_norm_experiment.py`, `advanced_validation.py`)는 `intention-link` 저장소의 `emg_data_collection/`에 있음.

---

## 1. 오늘의 최종 결론 (이것만 읽어도 됨)

### ✅ 발표에 쓸 수 있는 것
| # | 내용 | 근거 |
|---|---|---|
| 1 | 무작위분할 91.5% vs LOSO 86.9% → **4.6%p 과대평가** | 원 보고서 결과, 오늘 재현 확인 |
| 2 | 정규화·주파수특징·라벨정제·few-shot **4가지 모두 개선 없음** → 문제는 알고리즘이 아니라 데이터 조건 | 기존 s1~s4로 실험 |
| 3 | 수집 파이프라인 **버그 4개 발견·수정** (시리얼 7초 지연 / ADC 이상값 / 녹화 중 신호 붕괴 / 로봇 USB 간섭) | 각각 재현·수정 완료 |
| 4 | **한 조건으로만 학습한 모델은 다른 조건에서 붕괴(0.643, 전부 grip 오판) → 여러 조건 섞어 학습하면 99.6%** | 오늘 수집 v5/v6/v7, Leave-One-Session-Out |
| 5 | **실물 AmazingHand를 EMG로 구동 성공, 영상 확보** | `main.py --calibrate --hand-port` |

### ❌ 쓰면 안 되는 것 (오늘 중 폐기)
- "라벨 정제로 95% 달성" — 학습·테스트 둘 다 정제해서 시험을 쉽게 만든 것. 학습만 정제하면 오히려 하락(86.9→83.6)
- "세션 바뀌면 92.8→64.3" — 기존 데이터(100Hz)와 오늘 데이터(500Hz)는 펌웨어가 달라 비교 무효
- "전극 재부착이 모델을 무너뜨린다" — 재부착만 한 B↔C는 0.985~1.000으로 멀쩡. 붕괴 원인은 로봇 연결에 따른 잡음 수준 변화

### ⚠️ 정직하게 명시할 것
- 세션 실험은 **1인·3세션**
- s3 신호 붕괴(4092→42)는 "피로 **또는** 접촉 불량 추정"
- **ML 경로(`final_model.npz`)는 옛 100Hz 펌웨어 스케일로 학습돼 현재 새 펌웨어에서 오작동** (`ml=GRIP p=1.00` 고정). 로봇 시연은 임계값 경로 + `--calibrate`로 동작. **11월 전 재학습 필요**

### 한 줄 요약
> 정확도를 올리려고 알고리즘을 만졌지만 안 됐고, 대신 **데이터를 어떤 조건에서 받느냐가 모델의 생사를 가른다**는 걸 실험으로 확인했으며, 그 조건을 포괄해 학습하면 해결된다는 것까지 보였다. 그리고 로봇손이 실제로 움직인다.

---

## 2. 핵심 수치

### 2-1. 기존 s1~s4 (원 보고서 데이터, 100Hz)
| 방법 | LogReg | RF |
|---|---|---|
| 무작위 분할 | 91.5% | — |
| LOSO (기준) | **86.9%** | **89.2%** |
| + %MVC 정규화 | 87.2% | 87.8% |
| + 주파수 특징 | 87.5% | 88.1% |
| + few-shot 20% | 87.5% | 89.3% |
| 학습만 정제(상위50%) | 83.6% ↓ | 86.9% ↓ |
| 4클래스(REST/LIGHT/STRONG/GRIP, 의사라벨) | 76.7% | 80.6% |

피험자별 grip 진폭: s1 1,321 / s2 2,599 / s3 1,108 / s4 1,419 → 최대 **2.3배** 차이

### 2-2. 오늘 수집 세션 (s1 본인, 500Hz, 같은 펌웨어)
| 세션 | 로봇 USB | rest 기준선 | rest 잡음(std) | grip | 차이 |
|---|---|---|---|---|---|
| A (v5) | **분리** | 199 | **5.9** | 1,074 | 875 |
| B (v6) | 연결 | 1,298 | **19.5** | 1,752 | 454 |
| C (v7) | 연결 | 2,227 | **19.5** | 2,234 | 7 |

**한 세션 학습 → 다른 세션 평가 (RF)**
| | 결과 | 실제 예측 |
|---|---|---|
| A→B, A→C | **0.643** | grip 100% — rest를 하나도 못 알아봄 |
| B→A / B→C / C→A / C→B | 0.999 / 0.985 / 0.996 / 1.000 | 정상 |

특징을 모양 위주(std, zc)로 바꿔도 A→B는 여전히 0.643 → **특징 문제가 아님**

**두 세션 섞어 학습 → 나머지 평가 (Leave-One-Session-Out)**
| 평가 세션 | LogReg | RF |
|---|---|---|
| A | 0.995 | 0.998 |
| B | 0.996 | 1.000 |
| C | 0.998 | 0.912 |
| **평균** | **0.996** | 0.970 |

**해석**: 잡음 수준(5.9 vs 19.5)이 로봇 연결 여부와 일치. 조용한 조건으로 학습한 모델은 rest 기준이 낮아 시끄러운 조건의 rest를 전부 grip으로 판정. 전극 재부착(B→C)은 기준선을 900 옮겼지만 잡음 특성이 같아 모델이 견딤.

**설계 원칙**: 학습·보정 데이터는 실사용 조건(로봇 연결 상태, 착용 회차)을 포괄해야 한다. → 11월 시연은 로봇 연결 상태이므로 **데이터도 반드시 로봇 연결 상태에서 받을 것**.

---

## 3. 오늘 타임라인

| 시간 | 한 것 | 결과 |
|---|---|---|
| 오전 | 보고서·저장소 분석, Unity 제외 결정, 제출 요건(11p PDF + 5분 MP4) 확인 | 방향 확정 |
| 13~15시 | 기존 데이터로 정규화/주파수/정제/few-shot 실험 | 전부 개선 없음. "95%"는 폐기 |
| 15~17시 | 새 수집 시도 → 센서 3,680 고정 → INA219 스케치 착오 → 펌웨어 재업로드 → 여전히 고정 → **로봇 USB 분리하니 정상** | 간섭 원인 규명 |
| 17시 | v3·v4 수집 → 앞부분이 rest 수준 → **시리얼 버퍼 7초 지연** 발견·수정 → ADC 이상값 필터 | 수집 도구 완성 |
| 17시 | v5 수집 (깨끗) → 기존 데이터와 비교 92.8→64.3 → **펌웨어 다름(100Hz vs 500Hz)** 확인 → 비교 무효 | 오류 교정 |
| 18시 | 로봇 재연결 → 기준선 상승하나 반응 유지 → `main.py --calibrate` 구동 → **로봇손 동작, 영상 촬영** | 10쪽 확보 |
| 18~19시 | v6·v7 수집(전극 재부착) → 교차 검증 → 다중 세션 학습 99.6% | 8·9쪽 확보 |
| 19시 | 논리 재검토 → v5만 로봇 분리였음 → 원인을 "전극 재부착"에서 "잡음 조건"으로 교정 | 최종 결론 |

---

## 4. 파일 목록

### 4-1. 코드 (전부 `~/Desktop/intention-link/emg_data_collection/`)
| 파일 | 상태 | 설명 |
|---|---|---|
| `collect.py` | **수정 (최종)** | 반복 수집, lead-in, 백그라운드 수신(버퍼 지연 해결), ADC 필터, 피로 감시. 전문은 §5-1 |
| `sensor_check.py` | **신규** | 10초 센서 점검. 잡음 대비 상승폭으로 판정. 전문은 §5-2 |
| `mvc_norm_experiment.py` | 신규(분석) | %MVC 정규화 / few-shot / 주파수특징 실험 (§2-1 결과) |
| `advanced_validation.py` | 신규(분석) | 학습만 정제 / 정제비율 민감도 / few-shot 곡선 (95% 폐기 근거) |
| 세션 교차검증 | §5-3에 수록 | v5/v6/v7 교차·LOSO-session (§2-2 결과) |

### 4-2. 로봇 코드 (`~/intention-link-robot/`, GitHub `baehyeon0420-star/intention-link-robot`)
- 오늘 `--continuous`(비례제어) 기능을 추가했다가 **버그 발견 후 전부 되돌림**. `git status` clean, 커밋 9e4a334 상태 그대로.
- 버그 내용: `apply_ratio()`가 매 루프(50ms)마다 서보 8개에 16회 시리얼 쓰기 → 버스 포화 → 손이 굳음. 다시 넣을 땐 값이 변했을 때만 쓰기 + 서보 명령 주기 분리 + 재시도 축소 필요.
- 실물 구동 명령(검증됨):
  ```bash
  cd ~/intention-link-robot
  python3 main.py --port /dev/cu.usbserial-110 --calibrate --hand-port /dev/cu.usbmodem5B790178941
  ```

### 4-3. 데이터 (`~/Desktop/intention-link/emg_data_collection/`)
| 파일 | 상태 | 비고 |
|---|---|---|
| `s1~s4_{rest,grip}.csv` | 원본 | 100Hz, 원 보고서 데이터. 오늘 데이터와 **섞어서 비교 금지** |
| `s1_{rest,grip}_v2.csv` | ❌ 무효 | 센서 죽은 상태(std 5) |
| `s1_{rest,grip}_v3.csv`, `_v4.csv` | ❌ 무효 | 시리얼 버퍼 지연 버그 상태에서 수집 |
| `s1_{rest,grip}_v5.csv` | ✅ 세션 A | 500Hz, 로봇 **분리**, rest 199 |
| `s1_{rest,grip}_v6.csv` | ✅ 세션 B | 500Hz, 로봇 연결, rest 1,298 (rest는 실수분 제거 후 5,004샘플) |
| `s1_{rest,grip}_v7.csv` | ✅ 세션 C | 500Hz, 로봇 연결, rest 2,227 |
| `s1_grip_sustained10s.csv` | 부수 | v6 rest 수집 중 실수로 10초 연속 수축한 것. 피로 분석용으로 보관 |

v2~v4는 삭제 권장 (분석 스크립트는 `s*_rest.csv`/`s*_grip.csv`만 읽으므로 당장은 섞이지 않음).

### 4-4. 펌웨어
- `~/intention-link-robot/esp32_firmware/emg_myoware_reader/emg_myoware_reader.ino` — 현재 ESP32에 올라간 것. `analogRead(34)`, 12bit, 2ms(500Hz), `"millis,raw"` 출력.
- **원 보고서 s1~s4를 받은 펌웨어(100Hz)는 어디에도 저장돼 있지 않음.** 이후 모든 펌웨어는 저장소에 커밋할 것.

### 4-5. 하드웨어 메모
- ESP32(EMG): `/dev/cu.usbserial-110` / AmazingHand: `/dev/cu.usbmodem5B790178941`
- MyoWare VIN → **3.3V** (5V 금지), ENV → GPIO34, REF 전극은 손목뼈
- **로봇 USB를 연결하면 EMG rest 잡음이 3배(5.9→19.5)로 뛰고 기준선이 오름.** 동작은 하나 분해능 저하. 근본 해결은 무선화(BLE/ESP-NOW) 또는 USB 아이솔레이터.
- Arduino IDE 시리얼 모니터와 Python은 포트를 동시에 못 씀 — 하나만 열 것.

---

## 5. 코드 전문

### 5-1. `collect.py` (최종)
```python
"""
EMG 원시 신호 수집.

개선된 프로토콜(2026-09-12): 한 번에 길게 받으면 근피로로 뒤로 갈수록 신호가
빠져서(s3 사례: 2587 -> 672 -> 356 -> 371) 라벨이 오염된다. 그래서
"짧게 여러 번 + 사이에 휴식"으로 나눠 받고, 매 회차 평균을 즉시 찍어서
힘이 빠지고 있으면 수집 중에 바로 알 수 있게 한다.

사용 예:
  python3 collect.py --subject s5 --gesture rest --seconds 10 --reps 1 --lead-in 0
  python3 collect.py --subject s5 --gesture grip --seconds 3 --reps 6 --rest-between 5

저장: "{subject}_{gesture}.csv" (있으면 이어쓰기)
"""

import argparse
import csv
import os
import statistics
import threading
import time

import serial


class SerialPump:
    """포트를 연 순간부터 쉬지 않고 읽어서 버퍼가 밀리지 않게 하는 백그라운드 리더.

    (2026-09-12) 기존에는 sleep 후 reset_input_buffer()로 비우려 했는데, macOS
    USB-시리얼 드라이버 버퍼까지는 비워지지 않아서 측정 시작 직후 7초치 과거
    데이터가 먼저 읽혔다. 그래서 serial_reader.py처럼 항상 읽어 두는 구조로 바꿈.
    """

    def __init__(self, ser):
        self.ser = ser
        self._buf = []
        self._capturing = False
        self._lock = threading.Lock()
        self._running = True
        self._thread = threading.Thread(target=self._loop, daemon=True)
        self._thread.start()

    def _loop(self):
        while self._running:
            try:
                line = self.ser.readline().decode(errors="ignore").strip()
            except Exception:
                continue
            if "," not in line:
                continue
            t, raw = line.split(",", 1)
            try:
                v = int(raw)
            except ValueError:
                continue
            # ESP32 ADC는 12비트(0~4095). 깨진 줄에서 나온 범위 밖 값은 버린다.
            if not (0 <= v <= 4095):
                continue
            with self._lock:
                if self._capturing:
                    self._buf.append(([t, raw], v))

    def capture(self, seconds):
        """지금부터 seconds 동안 들어오는 '실시간' 데이터만 모은다."""
        with self._lock:
            self._buf = []
            self._capturing = True
        time.sleep(seconds)
        with self._lock:
            self._capturing = False
            out = list(self._buf)
        return [r for r, _ in out], [v for _, v in out]

    def stop(self):
        self._running = False
        self._thread.join(timeout=0.5)


def parse_args():
    p = argparse.ArgumentParser(description="EMG raw 신호 수집 (반복 + 피로 감시)")
    p.add_argument("--port", default="/dev/cu.usbserial-110")
    p.add_argument("--baud", type=int, default=115200)
    p.add_argument("--subject", required=True, help="예: s1, s5 ...")
    p.add_argument("--gesture", required=True, help="예: rest, grip, light50 ...")
    p.add_argument("--seconds", type=float, default=3.0, help="1회당 수집 시간(초)")
    p.add_argument("--reps", type=int, default=1, help="반복 횟수")
    p.add_argument("--rest-between", type=float, default=5.0, help="회차 사이 휴식(초)")
    p.add_argument("--countdown", type=int, default=3, help="각 회차 시작 전 준비시간(초)")
    p.add_argument(
        "--lead-in",
        type=float,
        default=1.0,
        help="'쥐세요' 신호 후 실제 측정을 시작하기까지 기다리는 시간(초). "
        "힘을 주는 상승 구간이 데이터에 섞이지 않게 하기 위함. rest 수집 땐 0으로.",
    )
    return p.parse_args()


def main():
    args = parse_args()
    fname = f"{args.subject}_{args.gesture}.csv"

    ser = serial.Serial(args.port, args.baud, timeout=1)
    time.sleep(2)  # ESP32 리셋 대기
    pump = SerialPump(ser)  # 이 시점부터 계속 읽어서 버퍼가 밀리지 않게 함

    all_rows = []
    rep_means = []

    print(f"\n=== {args.subject} / {args.gesture} : {args.seconds:.0f}초 x {args.reps}회 ===")
    for rep in range(1, args.reps + 1):
        print(f"\n[{rep}/{args.reps}] 준비...")
        for i in range(args.countdown, 0, -1):
            print(f"   {i}")
            time.sleep(1)
        if args.lead_in > 0:
            # 먼저 쥐게 하고, 힘이 다 올라온 뒤에 측정을 시작한다.
            print(f"   ▶ 지금 쥐세요!")
            time.sleep(args.lead_in)
            print(f"   ● 측정 중... ({args.seconds:.0f}초) — 그대로 유지")
        else:
            print(f"   ▶ 시작! ({args.seconds:.0f}초)")

        rows, values = pump.capture(args.seconds)
        all_rows.extend([r + [args.subject, args.gesture] for r in rows])

        if values:
            mean = statistics.mean(values)
            rep_means.append(mean)
            drop = ""
            if len(rep_means) > 1:
                ratio = mean / rep_means[0] if rep_means[0] else 0
                drop = f"  (1회차 대비 {ratio*100:.0f}%)"
                if ratio < 0.7:
                    drop += "  ⚠️ 힘이 빠지고 있음 — 더 쉬었다 하세요"
            print(f"   ✔ {len(values)}샘플, 평균={mean:.0f}{drop}")
        else:
            print("   ⚠ 수신된 데이터가 없습니다. 연결 확인 필요")

        if rep < args.reps and args.rest_between > 0:
            print(f"   ... {args.rest_between:.0f}초 휴식 (힘 완전히 빼세요)")
            time.sleep(args.rest_between)

    pump.stop()
    ser.close()

    file_exists = os.path.exists(fname)
    with open(fname, "a", newline="") as f:
        w = csv.writer(f)
        if not file_exists:
            w.writerow(["t_ms", "raw", "subject", "gesture"])
        w.writerows(all_rows)

    print(f"\n=== 완료: {len(all_rows)}샘플 -> {fname} ===")
    if len(rep_means) > 2:
        print(f"회차별 평균: {[f'{m:.0f}' for m in rep_means]}")

        # 회차 간 힘이 들쑥날쑥한 것 자체는 문제가 아니다(실사용에서도 매번 다르다).
        # 진짜 문제는 (1) 피로로 회차가 갈수록 단조롭게 무너지는 것,
        #            (2) 특정 회차가 다른 회차 대비 붕괴해서 라벨이 거짓이 되는 것.
        n = len(rep_means)
        xs = list(range(n))
        mx, my = sum(xs) / n, sum(rep_means) / n
        cov = sum((x - mx) * (y - my) for x, y in zip(xs, rep_means))
        vx = sum((x - mx) ** 2 for x in xs) ** 0.5
        vy = sum((y - my) ** 2 for y in rep_means) ** 0.5
        corr = cov / (vx * vy) if vx and vy else 0.0

        med = statistics.median(rep_means)
        worst = min(rep_means)

        # 방향(상관)만 보면 20% 정도 완만히 내려가는 정상 케이스도 걸린다.
        # s3의 붕괴는 4092 -> 42 (99% 감소)였으므로, 크기(마지막/처음)도 같이 본다.
        decline = rep_means[-1] / rep_means[0] if rep_means[0] else 1.0
        print(f"피로 추세(회차-평균 상관): {corr:+.2f}, 마지막/처음 = {decline:.2f}", end="")
        if corr <= -0.7 and decline < 0.6:
            print("  ⚠️ 회차가 갈수록 무너지고 있습니다 (s3 사례와 같은 패턴) — 다시 받으세요")
        elif corr <= -0.7:
            print("  (완만한 하강이지만 크기는 허용 범위)")
        else:
            print("  (추세 없음 = 정상)")

        print(f"최약 회차 / 중앙값: {worst / med:.2f}", end="")
        if worst < med * 0.4:
            print("  ⚠️ 유독 약한 회차가 있습니다 — 그 회차는 grip이라 보기 어려움")
        else:
            print("  (모든 회차가 grip 수준 유지 = 정상)")
    print()


if __name__ == "__main__":
    main()
```

### 5-2. `sensor_check.py` (최종)
```python
"""
EMG 센서가 살아있는지 10초 만에 확인하는 진단 도구.
본격 수집 전에 이걸 먼저 돌려서, 신호가 실제로 근육에 반응하는지 확인한다.

판정: '비율'이 아니라 '잡음 대비 상승폭'으로 한다.
      직류 오프셋이 깔린 신호에서는 비율 기준이 무의미하기 때문.

사용법:
  python3 sensor_check.py --port /dev/cu.usbserial-110
"""

import argparse
import statistics
import time

import serial


def parse_args():
    p = argparse.ArgumentParser(description="EMG 센서 상태 점검")
    p.add_argument("--port", default="/dev/cu.usbserial-110")
    p.add_argument("--baud", type=int, default=115200)
    p.add_argument("--seconds", type=float, default=5.0, help="각 단계 측정 시간(초)")
    return p.parse_args()


def measure(ser, seconds, label):
    ser.reset_input_buffer()
    values = []
    t0 = time.monotonic()
    last_print = 0.0
    while time.monotonic() - t0 < seconds:
        line = ser.readline().decode(errors="ignore").strip()
        if "," not in line:
            continue
        try:
            v = int(line.split(",", 1)[1])
        except ValueError:
            continue
        if not (0 <= v <= 4095):   # 깨진 줄에서 나온 범위 밖 값 무시
            continue
        values.append(v)
        now = time.monotonic()
        if now - last_print >= 0.25:
            last_print = now
            bar = "█" * int(v / 4095 * 40)
            print(f"\r   {label}: {v:5d} |{bar:<40s}|", end="", flush=True)
    print()
    return values


def stats(values):
    if not values:
        return None
    return {
        "n": len(values),
        "mean": statistics.mean(values),
        "std": statistics.pstdev(values) if len(values) > 1 else 0.0,
        "min": min(values),
        "max": max(values),
    }


def main():
    args = parse_args()
    ser = serial.Serial(args.port, args.baud, timeout=1)
    time.sleep(2)

    print("\n[1/2] 힘을 완전히 빼고 가만히 계세요")
    time.sleep(1)
    rest = stats(measure(ser, args.seconds, "REST"))

    print("\n[2/2] 이번엔 최대한 세게 주먹을 쥐세요")
    time.sleep(1)
    grip = stats(measure(ser, args.seconds, "GRIP"))

    ser.close()

    if rest is None or grip is None:
        print("\n❌ 데이터를 못 받았습니다. 포트/연결 확인 필요")
        return

    print("\n" + "=" * 52)
    print(f"  {'':6s} {'평균':>8s} {'표준편차':>9s} {'최소':>7s} {'최대':>7s}")
    print(f"  {'REST':6s} {rest['mean']:8.0f} {rest['std']:9.0f} {rest['min']:7d} {rest['max']:7d}")
    print(f"  {'GRIP':6s} {grip['mean']:8.0f} {grip['std']:9.0f} {grip['min']:7d} {grip['max']:7d}")
    print("=" * 52)

    separation = (grip["mean"] - rest["mean"]) / max(rest["std"], 1)
    headroom = 4095 - rest["mean"]

    problems, warnings = [], []
    if grip["std"] < 20:
        problems.append(f"GRIP 표준편차가 {grip['std']:.0f}뿐 → 살아있는 EMG가 아니라 고정 전압으로 보임")
    if separation < 3:
        problems.append(f"REST 잡음 대비 GRIP 상승폭이 {separation:.1f}배뿐 → 근육 반응이 묻혀 있음")
    if rest["mean"] > 1500:
        warnings.append(
            f"REST 기준선이 {rest['mean']:.0f}로 높습니다 (위쪽 여유 {headroom:.0f}). "
            f"동작은 하지만 표현 범위가 좁아 분해능이 떨어집니다 — 접지/간섭 확인 권장"
        )

    if problems:
        print("\n❌ 센서에 문제가 있습니다. 수집하지 마세요.\n")
        for p in problems:
            print(f"   · {p}")
        print("\n   확인: REF 전극 부착 상태 → 전극 3개 스냅 연결 → 젤 마름 여부 → ENV 핀 배선")
    else:
        print(f"\n✅ 근육 반응 정상. GRIP이 REST 잡음의 {separation:.1f}배만큼 상승 "
              f"(상승폭 {grip['mean']-rest['mean']:.0f}).")
        for w in warnings:
            print(f"   ⚠️  {w}")
    print()


if __name__ == "__main__":
    main()
```

### 5-3. 세션 교차 검증 스크립트 (§2-2 결과 재현용)
```python
# ~/Desktop/intention-link/emg_data_collection 에서 실행
import numpy as np, pandas as pd, itertools
from sklearn.ensemble import RandomForestClassifier
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import accuracy_score
from sklearn.preprocessing import StandardScaler

WIN, STEP = 20, 10
def extract(s):
    s = np.asarray(s, float)
    return [np.mean(np.abs(s)), np.sqrt(np.mean(s**2)), np.std(s),
            np.sum(np.abs(np.diff(s))), np.sum(np.diff(np.sign(s-s.mean()))!=0)]
def load(f):
    v = pd.to_numeric(pd.read_csv(f)["raw"], errors="coerce").dropna()
    return v[(v>=0)&(v<=4095)].values
def wins(raw): return [extract(raw[i:i+WIN]) for i in range(0,len(raw)-WIN,STEP)]
def full(v):
    r,g = load(f"s1_rest_{v}.csv"), load(f"s1_grip_{v}.csv")
    X = wins(r)+wins(g); y=[0]*len(wins(r))+[1]*len(wins(g))
    return np.array(X), np.array(y)
def fit_eval(Xtr,ytr,Xte,yte,Model=RandomForestClassifier,kw=dict(n_estimators=100,random_state=42)):
    sc=StandardScaler().fit(Xtr); m=Model(**kw).fit(sc.transform(Xtr),ytr)
    p=m.predict(sc.transform(Xte)); return accuracy_score(yte,p), p.mean()

SES=["v5","v6","v7"]; D={v:full(v) for v in SES}

print("■ 세션 상태")
for v in SES:
    r,g=load(f"s1_rest_{v}.csv"),load(f"s1_grip_{v}.csv")
    print(f"   {v}: rest={r.mean():5.0f} std={r.std():4.1f}  grip={g.mean():5.0f}")

print("\n■ 한 세션 학습 → 다른 세션 평가")
for a,b in itertools.permutations(SES,2):
    acc,pg=fit_eval(*D[a],*D[b])
    print(f"   {a}→{b}: {acc:.3f} (grip 예측 {pg*100:.0f}%)")

print("\n■ Leave-One-Session-Out")
for hold in SES:
    tr=[v for v in SES if v!=hold]
    Xtr=np.vstack([D[v][0] for v in tr]); ytr=np.concatenate([D[v][1] for v in tr])
    lr,_=fit_eval(Xtr,ytr,*D[hold],Model=LogisticRegression,kw=dict(max_iter=1000))
    rf,_=fit_eval(Xtr,ytr,*D[hold])
    print(f"   {'+'.join(tr)} → {hold}: LogReg={lr:.3f} RF={rf:.3f}")
```

---

## 6. 남은 일

### 제출 전 (2일 내)
- [ ] 11p PDF 작성 — 구성안은 별도 정리본 참고 (5~10쪽 기술 파트 내용 확정됨)
- [ ] 5분 영상 편집 (오늘 촬영본 사용)
- [ ] 파일명: `[신한대학교] Intention Link_김정환` (팀장 성함 확인)
- [ ] 팀원 명단 4명 확인 (김정환/이다예/황배현/김나정)

### 11월 시연 전
- [ ] **ML 모델 재학습** — 새 펌웨어(500Hz), 로봇 연결 상태, 여러 착용 세션 데이터로
- [ ] 피험자 확대 (현재 세션 실험은 1인)
- [ ] 압력센서(FSR400 ×3) 연동 → 폐루프 파지 (접촉까지 닫기 / 힘 상·하한 / 래칭)
- [ ] 비례 제어(`--continuous`) 재구현 — 값 변화 시에만 쓰기, 서보 명령 주기 분리, 재시도 축소
- [ ] 무선화(BLE/ESP-NOW) 또는 USB 아이솔레이터 — 로봇 USB 접지 간섭 해결
- [ ] 펌웨어를 저장소에 커밋 (원본 100Hz 펌웨어 유실 재발 방지)
